#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../astify/node_modules/esprima/bin/esparse.js" "$@"
  ret=$?
else 
  node  "$basedir/../astify/node_modules/esprima/bin/esparse.js" "$@"
  ret=$?
fi
exit $ret
